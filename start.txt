-- ==========================================
-- НАСТРОЙКИ СКРИПТОВ (МЕНЯТЬ ЗДЕСЬ)
-- ==========================================
local pcScriptString = [[
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Kenderlike/script/refs/heads/main/scriptPC.lua"))()
]]

local mobileScriptString = [[
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Kenderlike/script/refs/heads/main/scriptPhone.lua"))()
]]
-- ==========================================

-- Сервисы
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

-- Удаляем старое GUI, если оно есть (для удобной перезагрузки)
local guiName = "MyLordPlatformSelector"
if CoreGui:FindFirstChild(guiName) then
    CoreGui[guiName]:Destroy()
end

-- Создание ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = guiName
ScreenGui.Parent = CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Главный фрейм
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
MainFrame.Position = UDim2.new(0.5, -150, 0.5, -100)
MainFrame.Size = UDim2.new(0, 300, 0, 200)
MainFrame.ClipsDescendants = true

local UICorner_Main = Instance.new("UICorner")
UICorner_Main.CornerRadius = UDim.new(0, 12)
UICorner_Main.Parent = MainFrame

-- Тень (через UIStroke)
local UIStroke = Instance.new("UIStroke")
UIStroke.Parent = MainFrame
UIStroke.Color = Color3.fromRGB(60, 60, 70)
UIStroke.Thickness = 1.5

-- Заголовок
local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Parent = MainFrame
Title.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 0, 0.05, 0)
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Font = Enum.Font.GothamBold
Title.Text = "ВЫБЕРИТЕ ПЛАТФОРМУ"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16

-- Разделительная линия
local Line = Instance.new("Frame")
Line.Name = "Line"
Line.Parent = MainFrame
Line.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
Line.Position = UDim2.new(0.05, 0, 0.25, 0)
Line.Size = UDim2.new(0.9, 0, 0, 2)
Line.BorderSizePixel = 0

-- Функция для создания красивых кнопок
local function createButton(name, text, yPos, color, hoverColor)
    local Button = Instance.new("TextButton")
    Button.Name = name
    Button.Parent = MainFrame
    Button.BackgroundColor3 = color
    Button.Position = UDim2.new(0.1, 0, yPos, 0)
    Button.Size = UDim2.new(0.8, 0, 0, 45)
    Button.Font = Enum.Font.GothamSemibold
    Button.Text = text
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.TextSize = 15
    Button.AutoButtonColor = false

    local UICorner = Instance.new("UICorner")
    UICorner.CornerRadius = UDim.new(0, 8)
    UICorner.Parent = Button

    -- Анимация наведения
    Button.MouseEnter:Connect(function()
        TweenService:Create(Button, TweenInfo.new(0.2), {BackgroundColor3 = hoverColor}):Play()
    end)
    Button.MouseLeave:Connect(function()
        TweenService:Create(Button, TweenInfo.new(0.2), {BackgroundColor3 = color}):Play()
    end)

    return Button
end

-- Кнопка ПК
local PCButton = createButton(
    "PCButton", 
    "💻 Запустить для ПК", 
    0.35, 
    Color3.fromRGB(88, 101, 242), -- Цвет Discord Blurple
    Color3.fromRGB(105, 116, 245)
)

-- Кнопка Телефон
local MobileButton = createButton(
    "MobileButton", 
    "📱 Запустить для Телефона", 
    0.65, 
    Color3.fromRGB(46, 204, 113), -- Мятно-зеленый
    Color3.fromRGB(56, 224, 128)
)

-- Логика перетаскивания (Drag)
local dragging, dragInput, dragStart, startPos
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Выполнение кода
local function executeAndClose(codeString)
    -- Закрываем GUI
    ScreenGui:Destroy()
    -- Выполняем код через loadstring
    local success, err = pcall(function()
        local func = loadstring(codeString)
        if func then
            func()
        else
            warn("Ошибка: loadstring вернул nil. Проверьте синтаксис кода.")
        end
    end)
    if not success then
        warn("Ошибка при выполнении скрипта: " .. tostring(err))
    end
end

PCButton.MouseButton1Click:Connect(function()
    executeAndClose(pcScriptString)
end)

MobileButton.MouseButton1Click:Connect(function()
    executeAndClose(mobileScriptString)
end)
